/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // !! ATTENTION !!
    // Cela permettra aux builds de se terminer avec succès
    // même si ton projet contient des erreurs TypeScript.
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
